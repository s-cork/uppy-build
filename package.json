{
  "name": "uppy-bundle",
  "version": "1.0.0",
  "description": "This is a module for Uppy, packaged for easy import and use in other Node projects.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node esbuild.config.mjs",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@uppy/aws-s3": "^3.x",
    "@uppy/core": "^3.x",
    "@uppy/dashboard": "^3.x"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
